# pkanban TODO

## Phase 1: Header & Footer Components (DONE)
- [x] Create Header.svelte - logo, nav links, auth buttons
- [x] Create Footer.svelte - columns, copyright, social links
- [x] Create PublicLayout.svelte - wraps marketing pages with Header/Footer

## Phase 2: Layout Refactor (DONE)
- [x] Update App.svelte to use PublicLayout for marketing pages
- [x] App pages (BoardsList, Board, Organizations, Admin) are now standalone
- [x] Removed Header/Footer duplication issues

## Phase 3: Public Pages (with Header/Footer)

### Existing (updated)
- [x] Landing (`/`) - uses PublicLayout
- [x] Docs (`/docs/*`) - uses PublicLayout
- [x] Login (`/login`) - uses PublicLayout
- [x] Pricing (`/pricing`) - uses PublicLayout
- [x] About (`/about`) - uses PublicLayout
- [x] Contact (`/contact`) - uses PublicLayout
- [x] Privacy (`/privacy`) - uses PublicLayout
- [x] Terms (`/terms`) - uses PublicLayout

## Phase 4: App Pages (standalone, no Header/Footer)
- [x] BoardsList (`/boards`) - standalone
- [x] Board (`/boards/:id`) - standalone
- [x] Organizations (`/organizations`) - standalone
- [x] Organization (`/organizations/:id`) - standalone
- [x] Admin (`/admin/*`) - standalone

---

## Done! Layout separation complete.

**Marketing pages** (`/`, `/login`, `/docs/*`, `/pricing`, `/about`, `/contact`, `/privacy`, `/terms`)
- Wrapped in PublicLayout
- Has Header and Footer

**App pages** (`/boards/*`, `/organizations/*`, `/admin/*`)
- Standalone components
- No Header/Footer
- Each has its own navigation
